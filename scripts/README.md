# Scripts

AEL (ADS macro language) or Python scripts for automation and post-processing.

Suggested contents:
- `export_plots.ael` — batch export of plots from ADS datasets
- `extract_specs.py` — parse `.ds` / exported CSV data and populate `results/summary_table.csv`
- `run_corners.ael` — automate PVT corner sweeps
